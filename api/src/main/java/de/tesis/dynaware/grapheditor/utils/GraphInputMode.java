package de.tesis.dynaware.grapheditor.utils;

public enum GraphInputMode {
	/**
	 * Navigate (scrolling, panning, ..) the graph editor with the primary input
	 * device (mouse, touchscreen, ..)
	 */
	NAVIGATION,
	
	/**
	 * Select one or more elements inside the graph editor using the primary input
	 * device (mouse, touchscreen, ..) 
	 */
	SELECTION;
}
